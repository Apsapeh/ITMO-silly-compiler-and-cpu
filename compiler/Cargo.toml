[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
isa = { version = "0.1.0", path = "../isa" }
termtree = "1.0.0"

